함수 - 이름 뒤에 소괄호, 작성된 코드의 주소값을 담고 있는 저장공간
    f           (x)        2x+1
   함수명      매개변수      리턴값

함수 선언
    def 함수명(매개변수, ...):
        실행할 문장
        return 리턴값

함수를 사용하는 목적
    1. 재사용의 목적
        ※ 절대 특정성을 부여해서는 안된다.

    2. 소스코드 간결화
